language: ruby
cache: bundler

rvm:
  - 2.2.2

addons:
  postgresql: '9.3'

before_script:
  - psql -c 'create database undp_travis_ci_test;' -U postgres
  - cp config/database.yml.travis config/database.yml

script: 
  - RAILS_ENV=test bundle exec rake db:setup --trace
  - 'bundle exec rake'

notifications:
  email:
    recipients:
      - sebastian.schkudlara@vizzuality.com
    on_failure: change
    on_success: never